import React from 'react'

function MyCompnents() {
    // 1.fuctional component: with the help of hooks only we can handle the states and life cycles
    // it is called stateless component

    // 2. class component: it have in built features to handle the states and life cycles 
    // it is called stateful component
    // syntax; class ComponentName extends React.Component(){
    // render(){    
    // return(<div></div>)}}

    // splitting components: A project or a single component can splitted in to multiple components.
    // composition:  merging or composite of components is called composition of components

  return (
    <div>MyCompnents</div>
  )
}

export default MyCompnents