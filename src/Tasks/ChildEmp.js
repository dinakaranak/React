import React from 'react'

function ChildEmp(props) {
  return (
    <div>
        <h1>{props.det.EmpName}</h1>
        <div>{props.det.EmpSalary}</div>
    </div>
  )
}

export default ChildEmp

// celcius to ferhenheit
// area of circle
